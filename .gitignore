# Secrets — never commit
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node (only if tooling is added later)
node_modules/
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Build / deploy artefacts
.netlify/
dist/
build/

# Generated PDFs from local test runs (keep the canonical samples in /pdfs)
scripts/output/
*.tmp.pdf
